# Vercel Deployment TODO

## Steps to complete:
- [x] 1. Remove localhost API rewrite from next.config.ts
- [x] 2. Create vercel.json configuration file

## After deployment:
- [ ] Set environment variables in Vercel dashboard (MONGODB_URI, JWT_SECRET, NEXT_PUBLIC_APP_URL)
- [ ] Deploy to Vercel using `npx vercel deploy` or connect GitHub repo
